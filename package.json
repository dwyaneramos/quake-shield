{
  "name": "quakeshield",
  "private": true,
  "scripts": {
    "dev": "turbo run dev dev:fuji",
    "build": "turbo build",
    "test": "turbo test",
    "oracle": "pnpm --filter oracle dev",
    "oracle:fuji": "pnpm --filter oracle dev:fuji",
    "deploy:contracts": "pnpm --filter contracts deploy",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
